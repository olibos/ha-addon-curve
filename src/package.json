{
  "name": "ha-addon-curve",
  "version": "0.1.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun index.ts",
    "build": "bun build index.ts --outdir ../dist --target node --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^4.17.21"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}